[package]
name = "ide"
version = "0.1.0"
edition = "2024"

[dependencies]
iced-code-editor = "0.3.7"
iced = { version = "0.14", features = ["tokio", "canvas", "svg", "image"] }
tokio = { version = "1", features = ["full"] }
